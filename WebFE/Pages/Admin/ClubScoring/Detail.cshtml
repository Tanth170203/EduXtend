@page
@model WebFE.Pages.Admin.ClubScoring.DetailModel
@{
    ViewData["Title"] = "Club Details";
    Layout = "~/Pages/Shared/_AdminLayout.cshtml";
}

<!-- Breadcrumb -->
<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/Admin">Admin</a></li>
        <li class="breadcrumb-item"><a href="/Admin/ClubScoring">Club Scoring</a></li>
        <li class="breadcrumb-item active">Details</li>
    </ol>
</nav>

<!-- Page Header -->
<div class="page-header mb-4">
    <div class="d-flex align-items-center justify-content-between flex-wrap gap-3">
        <div>
            <h1 class="page-title">
                <i data-lucide="trophy" style="width: 28px; height: 28px; display: inline-block; vertical-align: middle; margin-right: 0.5rem;"></i>
                @Model.Record?.ClubName
            </h1>
            <p class="page-description">Month @Model.Month - @Model.Record?.SemesterName</p>
        </div>
        <div>
            <a href="/Admin/ClubScoring?semesterId=@Model.SemesterId&month=@Model.Month" class="btn btn-outline-secondary">
                <i data-lucide="arrow-left"></i> Back
            </a>
            <a class="btn btn-warning ms-2" href="/Admin/ClubScoring/ManualAdd?clubId=@Model.ClubId&semesterId=@Model.SemesterId&month=@Model.Month">
                <i data-lucide="plus" style="width: 16px; height: 16px;"></i>
                ➕ Add Score
            </a>
        </div>
    </div>
</div>

<!-- Club Info Card -->
<div class="card border-0 shadow-sm mb-3">
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                <strong>Club Name:</strong>
                <div>@Model.Record?.ClubName</div>
            </div>
            <div class="col-md-6">
                <strong>President:</strong>
                <div>@Model.Record?.PresidentName (@Model.Record?.PresidentCode)<br /><small class="text-muted">@Model.Record?.PresidentEmail</small></div>
            </div>
        </div>
    </div>
    <div class="card-footer bg-light">
        <div class="d-flex justify-content-between">
            <div><strong>Total (Month):</strong> <span class="text-primary fs-5">@Model.Record?.TotalScore</span> / 100</div>
        </div>
    </div>
</div>

<!-- Category Summary Card -->
<div class="card border-0 shadow-sm mb-3">
    <div class="card-header bg-info text-white d-flex align-items-center justify-content-center py-3">
        <i data-lucide="bar-chart-3" style="width: 20px; height: 20px; margin-right: 0.5rem;"></i>
        <strong>Total by Category (Meet / Event / Collab / Comp / Plan)</strong>
    </div>
    <div class="card-body">
        <div class="row g-3">
            <div class="col-md-2">
                <div class="p-3 border rounded text-center">
                    <div class="text-muted">Meet</div>
                    <div class="fs-5 fw-bold text-primary">@Model.Record?.ClubMeetingScore</div>
                </div>
            </div>
            <div class="col-md-2">
                <div class="p-3 border rounded text-center">
                    <div class="text-muted">Event</div>
                    <div class="fs-5 fw-bold text-primary">@Model.Record?.EventScore</div>
                </div>
            </div>
            <div class="col-md-2">
                <div class="p-3 border rounded text-center">
                    <div class="text-muted">Collab</div>
                    <div class="fs-5 fw-bold text-primary">@Model.Record?.CollaborationScore</div>
                </div>
            </div>
            <div class="col-md-2">
                <div class="p-3 border rounded text-center">
                    <div class="text-muted">Comp</div>
                    <div class="fs-5 fw-bold text-primary">@Model.Record?.CompetitionScore</div>
                </div>
            </div>
            <div class="col-md-2">
                <div class="p-3 border rounded text-center">
                    <div class="text-muted">Plan</div>
                    <div class="fs-5 fw-bold text-primary">@Model.Record?.PlanScore</div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Score Details Card -->
<div class="card border-0 shadow-sm">
    <div class="card-header bg-warning text-dark d-flex align-items-center justify-content-center py-3">
        <i data-lucide="clipboard-list" style="width: 20px; height: 20px; margin-right: 0.5rem;"></i>
        <strong>Scoring Details</strong>
    </div>
    <div class="card-body">
        @if (Model.Record?.Details?.Any() == true)
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead>
                        <tr>
                            <th><i data-lucide="tag" style="width: 16px; height: 16px; margin-right: 0.25rem;"></i>Type</th>
                            <th><i data-lucide="list" style="width: 16px; height: 16px; margin-right: 0.25rem;"></i>Criterion</th>
                            <th><i data-lucide="star" style="width: 16px; height: 16px; margin-right: 0.25rem;"></i>Score</th>
                            <th><i data-lucide="message-square" style="width: 16px; height: 16px; margin-right: 0.25rem;"></i>Note</th>
                            <th><i data-lucide="user" style="width: 16px; height: 16px; margin-right: 0.25rem;"></i>Evaluator</th>
                            <th><i data-lucide="calendar" style="width: 16px; height: 16px; margin-right: 0.25rem;"></i>Date</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var d in Model.Record.Details)
                        {
                            <tr>
                                <td>
                                    @if (d.ScoreType == "Auto")
                                    {
                                        <span class="badge bg-success">Auto</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-warning">Manual</span>
                                    }
                                </td>
                                <td>@d.CriterionTitle</td>
                                <td><strong class="text-primary">@d.Score</strong></td>
                                <td>@d.Note</td>
                                <td>
                                    @if (!string.IsNullOrEmpty(d.CreatedByName))
                                    {
                                        @d.CreatedByName
                                    }
                                    else
                                    {
                                        <span class="text-muted">System</span>
                                    }
                                </td>
                                <td>@d.AwardedAt.ToString("dd/MM/yyyy HH:mm")</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center text-muted py-5">
                <i data-lucide="inbox" style="width: 48px; height: 48px; margin-bottom: 1rem;"></i>
                <div>No scores available</div>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function(){
            if (typeof lucide !== 'undefined') lucide.createIcons();
        });

        function updateRangeHint(){
            const sel = document.getElementById('criterionSelect');
            const opt = sel.options[sel.selectedIndex];
            const min = opt.getAttribute('data-min');
            const max = opt.getAttribute('data-max');
            const score = document.getElementById('scoreInput');
            const hint = document.getElementById('rangeHint');
            if(min && max){
                score.setAttribute('min', min);
                score.setAttribute('max', max);
                hint.innerText = `Range: ${min}-${max} points`;
            }
        }
    </script>
}
